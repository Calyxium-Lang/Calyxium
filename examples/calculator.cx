let a = input("Enter value a: ") in
let b = input("Enter value b: ") in
let op = input("Enter operator: ") in

let safeDiv(a, b) {
    if b == 0.0 then panic("Can't divide by zero\n") else print(a / b)
}

match op with
    | '+' -> print(a + b)
    | '-' -> print(a - b)
    | '*' -> print(a * b)
    | '/' -> safeDiv(a, b)
    | _ -> print("Operator not supported")
