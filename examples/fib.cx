fib(n: int): int {
    rec aux(n: int, a: int, b: int): int {
        if n == 0 then a else aux(n - 1, b, a + b)
    }
    aux(n, 0, 1)
}

let result: int = fib(92)
if (result == 7540113804746346429) {
    println("true")
}