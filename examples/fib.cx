rec fib(n: int): int {
    rec aux(n: int, a: int, b: int): int {
        if n == 0 then a else aux(n - 1, b, a + b)
    }
    aux(n, 0, 1)
}

let result: int = fib(50)
if (result == 12586269025) {
    println("true")
}